{% extends "base.html" %}
{%  block main %}
<h1 class="custom_title" align="" id="header">Sign in</h1>
<form class="form-horizontal" method="post" action="{% url 'signi' %}?next={{ redirect }}" >
{% csrf_token %}
  <div class="control-group">
  {% if error %}
    <div class="alert alert-danger" role="alert">Incorrect user! Please try agan!</div>
  {% endif %}
    <label class="control-label" for="username"  >Username</label>
    <div class="controls">
      <input type="text" id="username" name="username" placeholder="Username" value="{% if username %}{{ username }}{% endif %}">
    </div>
  </div>
  <div class="control-group">
    <label class="control-label" for="password" >Password</label>
    <div class="controls">
      <input type="password" name="password" id="password" placeholder="Password" value="{% if password %}{{ password }}{% endif %}">
    </div>
  </div>
  <div class="control-group">
    <div class="controls">
      <label class="checkbox">
        <input type="checkbox"> Remember me
      </label>
  <div class="control-group">
      <a href="/signin">Forgot password</a>
      <a href="/signup">Create new</a>
  </div>
      <button type="submit" class="btn">Sign in</button>
    </div>
  </div>
</form>
{% endblock %}
